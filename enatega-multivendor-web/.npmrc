# react-chat-elements peer-pins react-dom@18.2.0 and does not yet support React 19.
# We intentionally run it on React 19 (used only in the chat-with-rider modal),
# so relax peer-dependency resolution to allow installs without --legacy-peer-deps.
legacy-peer-deps=true
